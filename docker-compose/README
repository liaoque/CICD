#### jenkins的插件加速， 如果实在无法下载 jenkins插件， 可尝试使用 docker-compose-nginx.yml 

使用方法
1. 把docker-compose-nginx.yml中 192.168.133.129 修改成 自己服务器的地址
2. 把docker-compose-nginx.yml替换原来的 docker-compose.yml
3. docker-compose down
4. docker-compose up -d
5. 复制 updates.jenkins-ci.org.conf 到 /alidata/server/nginx/conf.d/
6. docker-compose restart nginx
 